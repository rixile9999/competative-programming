#include <iostream>
#include <vector>
#include <map>
using namespace std;

int main() {
    int t, n, k, input;
    cin >> t;
    for (int i = 0; i < t; ++i) {
        cin >> n >> k;
        vector<int> a;
        for (int i = 0; i < n; ++i) {
            cin >> input;
        }
    }
}